doctype html
html(lang="pt-BR")
  head
    title Upload bem-sucedido
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css")
    link(rel="stylesheet" href="/css/custom.css")
  body
    // Navbar
    header.navbar.bg-dark.p-3.mb-4
      .container.d-flex.justify-content-center.align-items-center
        a.logo-text(href="/")
          span.text-white Projetos IOT

    // Container principal
    .container
      // Card exibindo o arquivo enviado
      .card.shadow-lg.mb-4
        .card-body
          h2.card-title.text-center Upload bem-sucedido!
          // Se o arquivo for uma imagem, exibe-o; caso contrário, exibe um link
          if fileName && fileName.match(/\.(jpg|jpeg|png|gif)$/i)
            img.img-fluid(src="/uploads/" + fileName, alt="Arquivo enviado")
          else
            p.text-center 
              a(href="/uploads/" + fileName) Visualizar arquivo

      // Botão de voltar para a página de envio
      .text-center.mt-3
        a.btn.btn-secondary(href="/send-files") Fazer mais envios